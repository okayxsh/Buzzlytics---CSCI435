{
  "name": "buzzlytics",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"python backend/main.py\" \"cd frontend && npm run dev\"",
    "backend": "python backend/main.py",
    "frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
